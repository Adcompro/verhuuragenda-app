import 'package:flutter/material.dart';

class AccommodationsListScreen extends StatelessWidget {
  const AccommodationsListScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text('Accommodaties'),
      ),
      body: const Center(
        child: Text('Accommodaties lijst - te implementeren'),
      ),
    );
  }
}
